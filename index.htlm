<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>NyxOraculum</title>
    <script src="https://telegram.org/js/telegram-web-app.js"></script>
    <style>
        :root {
            --primary: #2c3e50;
            --secondary: #3498db;
        }

        body {
            font-family: 'Arial', sans-serif;
            background: #f8f9fa;
            padding: 15px;
        }

        .card {
            background: white;
            border-radius: 15px;
            padding: 20px;
            box-shadow: 0 3px 6px rgba(0,0,0,0.1);
            margin: 10px 0;
            animation: fadeIn 0.5s ease;
        }

        .game-button {
            display: block;
            width: 100%;
            padding: 15px;
            margin: 10px 0;
            background: var(--secondary);
            color: white;
            border: none;
            border-radius: 8px;
            cursor: pointer;
            transition: transform 0.2s;
        }

        .game-button:hover {
            transform: translateY(-2px);
        }

        .symbol-grid {
            display: grid;
            grid-template-columns: repeat(3, 1fr);
            gap: 10px;
        }

        .symbol-item {
            font-size: 2em;
            padding: 15px;
            border: 2px solid var(--primary);
            border-radius: 8px;
            cursor: pointer;
            transition: all 0.3s;
        }

        .symbol-item:hover {
            background: var(--primary);
            color: white;
        }

        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(20px); }
            to { opacity: 1; transform: translateY(0); }
        }
    </style>
</head>
<body>
    <div class="card">
        <h1 style="text-align: center; color: var(--primary);">🌀 NyxOraculum</h1>
        <button class="game-button" onclick="startGame('telepathy')">🔢 Телепатия чисел</button>
        <button class="game-button" onclick="startGame('zener')">🃏 Карты Зенера</button>
        <button class="game-button" onclick="startGame('color')">🎨 Цветовая интуиция</button>
        <button class="game-button" onclick="showStats()">📊 Статистика</button>
    </div>

    <div id="game-screen" class="card hidden"></div>
    <div id="achievements" class="card hidden"></div>
</body>
</html>